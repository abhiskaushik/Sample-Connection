<?php 

try {
	$db = new PDO("mysql:host=localhost;dbname=sampleConnection", "YOUR_USERNAME", "YOUR_PASSWORD");
    $db->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
}catch(PDOException $e) {
    die($e->getMessage());
}



?>
